<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:parentTag="android.widget.LinearLayout">

    <ImageView
        android:id="@+id/active_result_icon"
        android:layout_width="27dip"
        android:layout_height="27dip"
        android:layout_gravity="center"
        android:layout_marginStart="@dimen/activity_horizontal_margin"
        android:contentDescription="@null"
        android:src="@drawable/ic_active" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="10dip"
        android:orientation="vertical"
        android:padding="@dimen/margin_10">

        <TextView
            android:id="@+id/active_result"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/result_expired"
            android:textSize="@dimen/font_body"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/active_result_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/verification_credentialExpired"
            android:textColor="@color/subTextLabels"
            android:textSize="@dimen/font_body" />

    </LinearLayout>

</merge>